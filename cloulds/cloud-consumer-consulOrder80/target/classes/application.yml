server:
  port: 80

spring:
  application:
    name: consul-consumer-service #服务名称
  cloud:
    consul:
      host: localhost
      port: 8500
      discovery:
        service-name: ${spring.application.name}